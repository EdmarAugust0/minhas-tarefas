import styled from 'styled-components'

export const Container = styled.main`
  padding: 0 40px;
  /* esse dois abaixo forma para deixar que apenas o meio da página seja scrollada  */
  height: 100vh;
  overflow-y: scroll;
`
export const Resultado = styled.p`
  display: block;
  margin-top: 40px;
  font-size: 18px;
  font-weight: bold;
`
